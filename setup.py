# from distutils.core import setup
from setuptools import setup, find_packages

setup(name='xla_gpu',
      version='1.0',
      description='make tf 1.15 work with xla_gpu',
      author='test',
      author_email='test',
      url='',
      packages=find_packages(),
     )
